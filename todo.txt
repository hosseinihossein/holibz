- add reference element to other elements(and some other elements such as table, chart, list, ...)
- test edition and deletion of default library and shelf
- enable choosing heading for adding new element
- set review as lazy loading by defer
- launch notifications
- how to get more replies
- change downloading file and images of documents to use httpClient to have more control over it
- set angular app to show access denied message instead of application error
- 


- set changedetection onpush and do some other performance updates like ssr
- break the angular app to several sub apps for each service.
- add the ability for users to follow eachother
- add the ability for users to send review request to eachother for reading 
and reacting to their document.
- add table, list, chart and reference elements to documents
- create images with different sizes for each uploaded images
- upgrade to Angular v21
- add Async to the end of the name of async methods in models process class
- 